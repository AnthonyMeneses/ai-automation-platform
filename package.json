{
  "name": "ai-automation-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Secure SaaS dashboard + backend for a small-business AI automation platform",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm test --prefix backend",
    "build": "npm run build --prefix frontend",
    "migrate": "node backend/scripts/migrate.js",
    "seed": "node backend/scripts/seed.js",
    "create-admin": "node backend/scripts/create-admin.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
